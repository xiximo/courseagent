# 复制为 deploy/backend.env 后修改。
# DATABASE_URL / CORS_ORIGINS / JWT_SECRET 由 docker-compose 覆盖，这里只放应用其余配置。

APP_NAME=courseagent-backend
DEBUG=false
HOST=0.0.0.0
PORT=8080

JWT_EXPIRE_SECONDS=86400
INIT_ADMIN_USERNAME=admin
INIT_ADMIN_PASSWORD=change-me-admin-password

STORAGE_BACKEND=local
LOCAL_STORAGE_ROOT=/app/data/attachments
LOCAL_STORAGE_BUCKET=course-attachments
FAISS_INDEX_PATH=/app/data/vector_index
MILVUS_LITE_PATH=/app/data/milvus_qibiao.db

# compose 使用 Dockerfile.ml，模型目录挂载到 /app/models
EMBEDDING_MODE=inline
EMBEDDING_ENABLED=true
EMBEDDING_MODEL=models/bge-small-zh-v1.5
EMBEDDING_VECTOR_STORE=milvus
EMBEDDING_INDEX_BATCH_SIZE=64
INDEX_AUTO_ON_CHUNK=false
CHUNK_MAX_CHARS=1800
CHUNK_OVERLAP_CHARS=200

LLM_ENABLED=true
DOUBAO_MODEL_NAME=doubao-seed-2-0-pro-260215
DOUBAO_ENDPOINT_ID=
DOUBAO_API_KEY=
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
LLM_TIMEOUT_SECONDS=120
QA_RETRIEVAL_TOP_K=8

HARNESS_SCHEDULER_ENABLED=true
HARNESS_SCHEDULER_POLL_SECONDS=30

# 公网域名（Stripe 支付成功回跳、取消回套餐页都依赖这两项，不要留 localhost）
PUBLIC_BASE_URL=https://agent.rebuilder.com.cn
FRONTEND_BASE_URL=https://agent.rebuilder.com.cn
ALIPAY_SANDBOX=true
ALIPAY_ALLOW_MOCK=true
ALIPAY_APP_ID=
ALIPAY_APP_PRIVATE_KEY_PATH=
ALIPAY_PUBLIC_KEY_PATH=
# Stripe 测试密钥：https://dashboard.stripe.com/apikeys （测试模式 sk_test_...）
# 不要填 sk_live_。Webhook 演示可不填。
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CURRENCY=cny

DOCLING_WORKER_ENABLED=false
